#Defines bool_to_word function which takes one argument
def bool_to_word(boolean):
    #Checks if boolean is True
    if boolean == True:
        #Returns Yes
        return "Yes"
    #Returns No
    return "No"
